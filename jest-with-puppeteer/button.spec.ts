import {Button} from "./button";

describe('custom-button', () => {

    // let button: Button;

    beforeEach(() => {
       // button = new Button();
    });

   it('runs the test', () => {
       expect(true).toBe(true);
   })
});
